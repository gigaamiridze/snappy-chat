import { Request, Response, NextFunction } from 'express';

export const getAllMessage = (req: Request, res: Response, next: NextFunction) => {
  try {

  } catch (error) {
    next(error);
  }
}

export const addMessage = (req: Request, res: Response, next: NextFunction) => {
  try {
    
  } catch (error) {
    next(error);
  }
}