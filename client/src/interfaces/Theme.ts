export interface ITheme {
  colors: IColors;
  fonts: IFonts;
}

interface IColors {
  white: string;
  black: string;
  eerieBlack: string;
  strongBlack: string;
}

interface IFonts {
  primary: string;
}