import { AuthContainer } from '../components';
import { Brand } from '../layouts';

function Register() {
  return (
    <AuthContainer>
      <Brand />
    </AuthContainer>
  )
}

export default Register;